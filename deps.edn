{:paths ["src"]
 ;; junbi (準備) — etzhayyim reserve treasury actor (ADR-2607021800).
 ;; R0 is a pure .cljc core: no on-chain I/O, no vendor SDK. R1 wires
 ;; kotoba-lang/banking (double-entry), kotoba-lang/base-l2 (ERC-4337 reads)
 ;; and langgraph-clj (StateGraph + TreasuryGovernor) as :local/root deps,
 ;; same shape as the itonami / robotaxi / gftd-talent actors.
 :deps {org.clojure/clojure {:mvn/version "1.12.0"}}
 :aliases
 {:test {:extra-paths ["test"]
         :extra-deps  {io.github.cognitect-labs/test-runner
                       {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts   ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main" "--lint" "src" "test" "--fail-level" "error"]}}}
